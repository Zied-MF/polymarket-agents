/**
 * CLOB API - trading via l'API Central Limit Order Book de Polymarket
 *
 * Encapsule les appels à l'API CLOB (https://clob.polymarket.com).
 * Expose des fonctions pour :
 *   - créer des ordres limit/market sur un marché (buy YES / buy NO)
 *   - annuler des ordres ouverts
 *   - récupérer le carnet d'ordres en temps réel (bids/asks, spread)
 *   - consulter les positions et le solde du compte
 *   - suivre le statut de résolution d'un marché
 * Gère l'authentification par signature ECDSA (wallet Polygon).
 */

export {};
